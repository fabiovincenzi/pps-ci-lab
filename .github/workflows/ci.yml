name: CI
on:
  push:
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          java-version: 21
      - uses: actions/setup-java@v4
        run: ./gradlew clean build
